if checkpoint written (like for rimworld), needn't run through any detection or planning (& no terminal output), just launching the process with the existing proper configuration
sys tweaks don't seem persistent, need to check why


 

ppl just use steam
couldn't they use play for config through steam seamlessly, like with launch args "play %command%"

when quit, screen captured by game

huge drops occasionally