ppl just use steam
couldn't they use play for config through steam seamlessly, like with launch args "play %command%"

no clean action to quit game, must just use top right x button in desktop, amateurish. Need a proper minimal intuitive quick but safe way to stop the game. 

also consider modifying play cache directory names at ~/.local/share/play/cache/ from pure hashes to at least contain game name + unique identifiers for easier debugging, same goes for checkpoints

huge frame drops occasionally, need to investigate

need a workflow to update arch.md whenever major changes are made to project so that any llm can read it and be up to date

should system tweaks be persistent? it's a real pain to type passwords on every launch, maybe keep the safe ones and only prompt for unsafe?